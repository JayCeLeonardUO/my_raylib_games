# this will take in the img and turn it into a glb
import os
import subprocess


def main(args,argv):
    # take in the image path and turn it into a glb.
    # if you pass in a dir then it will turn all the images in the dir into glbs

